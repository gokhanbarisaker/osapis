<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    tools:ignore="MergeRootFrame"
    android:orientation="vertical"
    >

    <EditText android:id="@+id/edittext" android:layout_width="match_parent" android:layout_height="72dp"/>
    <Button
        android:id="@+id/edittext_showbutton"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:text="Show keyboard for edittext"/>
    <Button
        android:id="@+id/edittext_hidebutton"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:text="Hide keyboard for edittext"/>

</LinearLayout>
